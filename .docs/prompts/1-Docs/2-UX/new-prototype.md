Create a prompt for an LLM to create a prototype based on the current docs. Put the focus on the UX. The user has to build a coherent structure for the app based on the current needs. The UI is a successive phase.

# REQUIREMENTS:
- Focus on core user flows and information architecture
- Include realistic placeholder content

**SEED DOC**: `@.docs/seed-document.md`
